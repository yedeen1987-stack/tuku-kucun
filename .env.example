# 复制成 .dev.vars（本地调试）或用 `npx wrangler secret put` 设到线上。
# 这两个都不要提交到 Git。

# 会话签名密钥。随便生成一串长随机字符串，例如：
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# 给以后的本地系统推送拣货单用（PUT /api/import）。至少 24 位。
# 现在用不到可以先空着，空着就等于关闭这个接口。
IMPORT_TOKEN=
