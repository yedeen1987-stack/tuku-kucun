{
  "name": "textil134-picking",
  "version": "1.0.0",
  "private": true,
  "description": "拿货小程序 · 拣货单 + 工人手机端",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute textil134-picking --local --file=schema.sql",
    "db:remote": "wrangler d1 execute textil134-picking --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
